[project]
name = "argendata_datasets"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "polars>=1.35.2",
]

[dependency-groups]
test = [
    "pytest>=9.0.1",
    "python-dotenv>=1.2.1",
]
